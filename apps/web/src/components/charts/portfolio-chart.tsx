'use client'; import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis } from 'recharts';
const data = Array.from({length: 12}, (_, i) => ({ month: new Date(2026, i).toLocaleString('en',{month:'short'}), capital: 800000 + i*70000, interest: 32000 + i*2100 }));
export function PortfolioChart() { return <ResponsiveContainer width="100%" height={280}><AreaChart data={data}><defs><linearGradient id="g" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="#2563EB" stopOpacity={0.4}/><stop offset="95%" stopColor="#2563EB" stopOpacity={0}/></linearGradient></defs><CartesianGrid strokeDasharray="3 3" opacity={0.2}/><XAxis dataKey="month"/><Tooltip/><Area type="monotone" dataKey="capital" stroke="#2563EB" fill="url(#g)" strokeWidth={3}/></AreaChart></ResponsiveContainer>; }
